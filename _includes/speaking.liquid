<div class="col">
  <a href="{% if speaking.redirect %}{{ speaking.redirect }}{% else %}{{ speaking.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if speaking.img %}
        {%
          include figure.liquid
          loading="eager"
          path=speaking.img
          sizes = "250px"
          alt="speaking thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ speaking.title }}</h2>
        <p class="card-text">{{ speaking.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if speaking.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ speaking.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if speaking.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ speaking.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
