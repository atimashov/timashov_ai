<div class="col">
  <a href="{% if media.redirect %}{{ media.redirect }}{% else %}{{ media.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if media.img %}
        {%
          include figure.liquid
          loading="eager"
          path=media.img
          sizes = "250px"
          alt="media thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ media.title }}</h2>
        <p class="card-text">{{ media.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if media.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ media.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if media.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ media.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
